package edu.akarimin.week3;

/**
 * Sorting Complexity:
 * model: Decision Tree => can access info only through compares (Comparable)
 * cost: # of compares
 * upper bound: ~NLgN from mergesort
 * lower bound: Stirling's formula: height of decision tree >= log(N!) ~ NLgN
 * Optimal Algorithm: merge Sort
 * Compares ? is optimal.
 * Space ? is not optimal.
 */
public class Complexity {
}
